apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cnapp-zta-system
  namespace: argocd
spec:
  project: cnapp-zta
  source:
    repoURL: "https://YOUR_GIT_URL/cnapp-zta-hybrid.git"
    targetRevision: main
    path: gitops/zta-system-bundle
  destination:
    server: "https://kubernetes.default.svc"
    namespace: zta-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
